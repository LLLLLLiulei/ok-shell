export enum RemoteOrLocal {
  REMOTE = 'remote',
  LOCAL = 'local',
}

export enum SshOrSftp {
  SSH = 'ssh',
  SFTP = 'sftp',
}

export enum TerminalStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
  CONNECTING = 'connecting',
  ERROR = 'error',
}
